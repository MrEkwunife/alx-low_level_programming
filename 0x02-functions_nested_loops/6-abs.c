#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @n: The integer to be examined
 * Return: The magnitude of an intege
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
