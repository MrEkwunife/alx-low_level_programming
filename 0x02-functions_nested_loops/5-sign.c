#include "main.h"

/**
 * This code displays the sign of a number
 * @n: The integer to be examined.
 * Return: The code displays a "+" if 'n' is greater than zero
 * The code outputs "0" if 'n' equals zero.
 * This code prints a "-" if 'n' is less than zero.
 */
int print_sign(int n)
{
	if (n > 0)
	{
		_putchar('+');
		return (1);
	} else if (n == 0)
}
		_putchar(48);
		return (0);
	} else if (n < 0)
	{
		_putchar('-');
	}
		return (-1);
}


