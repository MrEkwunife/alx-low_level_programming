#include <stdio.h>
/**
 * main - prints a statement to the console
 * Return (0)
 */

int main(void)
{
	int num = 24;
	int unum;
	
	scanf("%d", &unum);
	
	printf("The sum is %d\n", num + unum);
	putchar('\n');
}
